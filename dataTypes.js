//A data type is a specific format of data the variable holds
//string is a collection of characters
let firstName='Majata';

//number is a numerical value
let ageInYears=21;

//BigInt represents large numerical values
let balance=1000000;

//boolean represents a state of true or false
let isSmart=true;

//the object type is a collection of key value pairs
const person = {
    firstName:'majata',
    lastName:'hichimi'
}

//the null dataType represents the absence of a value 
let isNothing = null;

//the undefined tyoe is a variable that does not contain a value assigned to the identifer
let x;

/*
the type of operator is one that allows us to identify the specific dtatype of a value in, 
Javascript */

typeof 100;