/**
 * DOM(Document Object Model)
 * Is used to access html elements 
 * the most common accessmethod is access by id
 */

/**
 * DOM API is a set of methods and properties that allow Javascript to alter HTML content
 * An API Method is an action you can perform on HTML
 * An API property is a value that you can access on an HTML element
 */

//the word(document) refers to the html document itself
//the function(.getElementById()) refers to the method
//the word(innerHTML) reters to a property

/**
 * A DOM API provides the ablity to
 * find and select elements
 * alter webpage content
 * listen for user input
 * Javascript uses the browser to access the DOM through the API
 */